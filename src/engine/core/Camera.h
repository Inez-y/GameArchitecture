#ifndef LASTCARRIAGE_CAMERA_H
#define LASTCARRIAGE_CAMERA_H

#pragma once

#include <SDL3/SDL.h>
struct Camera {
    float x = 0.0f;
    float y = 0.0f;
    float w = 0.0f;
    float h = 0.0f;
};
#endif //LASTCARRIAGE_CAMERA_H